#
# NOTE! Don't add files that are generated in specific
# subdirectories here. Add them in the ".gitignore" file
# in that subdirectory instead.
#
# NOTE! Please use 'git ls-files -i --exclude-standard'
# command after changing this file, to see if there are
# any tracked files which get ignored after the change.
#
# Normal rules
#
test
.*
*.o
*.o.*
*.a
*.s
*.ko
*.so
*.so.dbg
*.mod.c
*.i
*.lst
*.symtypes
*.order
*.elf
*.bin
*.gz
*.bz2
*.lzma
*.xz
*.lz4
*.lzo
*.patch
*.gcno
modules.builtin
Module.symvers
*.dwo
tags
tags.*

#
# Top-level generic files
#
/kernel-source/kernel/tags
/kernel-source/kernel/TAGS
/kernel-source/kernel/linux
/kernel-source/kernel/vmlinux
/kernel-source/kernel/vmlinuz
/kernel-source/kernel/System.map
/kernel-source/kernel/Module.markers
/kernel-source/build-kernel-out/*
/uboot-source/build-uboot-out
/nanddisk-source/build-nanddisk-out

/burn-image-out/dtb
/burn-image-out/zImage

#
# git files that we don't want to ignore even it they are dot-files
#
!.gitignore
!/kernel-source/build-kernel-out/.config
!RTOSDemo.bin


# stgit generated dirs
patches-*

# quilt's files
patches
series

# cscope files
cscope.*
ncscope.*

# gnu global files
GPATH
GRTAGS
GSYMS
GTAGS

*.orig
*~
\#*#

#
# Leavings from module signing
#
extra_certificates
signing_key.priv
signing_key.x509
x509.genkey

# Kconfig presets
all.config
